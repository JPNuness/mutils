{
	"name": "@mutils/vue-utils",
	"version": "0.0.1",
	"description": "Set of TypeScript-based Vue-focused utility functions.",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "tsc src dist -d",
		"clean": "rimraf dist",
		"clean:full": "rimraf dist node_modules"
	},
	"files": [
		"dist"
	],
	"dependencies": {
		"@mutils/core-utils": "workspace:*"
	},
	"peerDependencies": {
		"vue": "^3.5.13"
	}
}
